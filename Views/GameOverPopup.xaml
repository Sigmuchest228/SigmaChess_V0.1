<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               x:Class="SigmaChess.Views.GameOverPopup"
               CanBeDismissedByTappingOutsideOfPopup="False"
               Color="Transparent">
    <toolkit:Popup.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Styles/Colors.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </toolkit:Popup.Resources>
    <Border Padding="22"
            Background="{StaticResource GlassCardBrush}"
            Stroke="{StaticResource GlassStrokeBrush}"
            StrokeShape="RoundRectangle 18"
            StrokeThickness="1"
            WidthRequest="320">
        <Border.Shadow>
            <Shadow Brush="#88000000"
                    Offset="0,12"
                    Opacity="0.38"
                    Radius="22" />
        </Border.Shadow>
        <VerticalStackLayout Spacing="16">
            <Label FontAttributes="Bold"
                   FontSize="18"
                   Text="Game over"
                   TextColor="{StaticResource GlassTitleText}" />
            <Label x:Name="ResultLabel"
                   FontSize="15"
                   LineBreakMode="WordWrap"
                   TextColor="{StaticResource GlassSubtitleText}" />
            <Grid ColumnDefinitions="*,*"
                  ColumnSpacing="10">
                <Button Grid.Column="0"
                        Padding="14,10"
                        Background="{StaticResource GlassPrimaryButtonBrush}"
                        BorderWidth="0"
                        Clicked="OnNewGameClicked"
                        CornerRadius="14"
                        Text="New game"
                        TextColor="#16161C" />
                <Button Grid.Column="1"
                        Padding="14,10"
                        Background="{StaticResource GlassNavButtonBrush}"
                        BorderWidth="0"
                        Clicked="OnHomeClicked"
                        CornerRadius="14"
                        Text="Home"
                        TextColor="{StaticResource GlassTitleText}" />
            </Grid>
        </VerticalStackLayout>
    </Border>
</toolkit:Popup>
