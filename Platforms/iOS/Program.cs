using ObjCRuntime;
using UIKit;

namespace SigmaChess
{
    // Стандартная точка входа iOS-приложения, сгенерированная MAUI-шаблоном.
    // Просто запускает UIKit и просит его использовать наш AppDelegate.
    public class Program
    {
        static void Main(string[] args)
        {
            UIApplication.Main(args, null, typeof(AppDelegate));
        }
    }
}
